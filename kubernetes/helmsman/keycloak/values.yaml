auth:
  tls:
    enabled: true
    autoGenerated: true

service:
  type: ClusterIP

metrics:
  enabled: true

ingress:
  enabled: true
  ingressClassName: nginx
  hostname: auth.incidentcad.com
  tls: true
  selfSigned: false
  annotations: 
    cert-manager.io/cluster-issuer: "incidentcad.com"

#extraStartupArgs: |
#  -Dkeycloak.frontendUrl=https://auth.incidentcad.com